apiVersion: v1
kind: Secret
metadata:
  name: {{ include "url-shortener.fullname" . }}-secret
type: Opaque
data:
  # Base64-encoded at deploy time via --set or external-secrets operator
  db-password: ""   # Override: --set dbPassword=<base64>
