apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-shortener.fullname" . }}
  labels:
    app: {{ include "url-shortener.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "url-shortener.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
